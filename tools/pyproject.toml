[project]
name = "bitwark-tools"
version = "0.1.0"
description = "Python test harness for the Bitwark chess engine"
requires-python = ">=3.12"
dependencies = [
    "chess>=1.11",  # python-chess: test-side FEN/move assertions only
]

[tool.uv]
package = false
